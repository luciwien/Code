bind mouse5 noclip
bind t say .clear

BIND F1 say .rethrow 0
bind F2 say .rethrow 1
bind F3 say .rethrow 2
bind F4 say .rethrow 3
bind F5 say .rethrow 4

alias pv say .preview
alias xr say .xray
alias nf say .noflash
alias imp say .noflash

echo " //=============================//"
echo " // .noflash NF | .impacts IMP |//"
echo " // .preview PV | .xray  XR    |//"
echo " //=============================//"

say n = bot || F1-F5 rethrow ||  m5 noclip || t clear || j savepos || bind h goto || pv for preview Off || xr for xray

bind y say .savepos //Z
bind h say .goto	
bind n say .bot
bind m say .dismiss
bind j say .p; 
bind k say .replay; 
bind l say .n;

echo " //=====================================================//"
echo " // Z savepos | U savepos   | I info     | O           | //"
echo " // H goto    | J .p (prev) | K .replay  | L .n (next) | //"
echo " // N botadd  | M botrem    | ,          | .           | //"
echo " //=====================================================//"

bind leftarrow say .usp
bind uparrow say .deagle
bind downarrow say .ak
bind rightarrow	say .m4

bind INS say .p2000
bind HOME say .glock
bind PGUP say .galil
bind DEL say .scout
bind END say .m4a4
bind PGDN say .famas

