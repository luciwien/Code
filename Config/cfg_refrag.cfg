bind mouse5 noclip
bind t say .clear
bind alt noclip

BIND F1 say .rethrow 0
bind F2 say .rethrow 1
bind F3 say .rethrow 2
bind F4 say .rethrow 3
bind F5 say .rethrow 4



alias pv say .preview
alias xr say .xray
alias nf say .noflash
alias imp say .impacts

echo " //=============================//"
echo " // .noflash NF | .impacts IMP |//"
echo " // .preview PV | .xray  XR    |//"
echo " //=============================//"

say aliases: pv - preview || xr - xray || nf - noflash || imp - impacts

bind y say .savepos //Z
bind u say im doing nothing
bind i say .impact;
bind o say .noflash;

bind h say .goto	
bind j say .p; 
bind k say .replay; 
bind l say .n;

bind n say .bot
bind m say .dismiss
bind , say .preview;
bind . say .xray;

echo " //=====================================================//"
echo " // Z savepos | U           | I impact   | O noflash   | //"
echo " // H goto    | J .p (prev) | K .replay  | L .n (next) | //"
echo " // N botadd  | M botrem    | , noflash  | . xray      | //"
echo " //=====================================================//"

bind leftarrow say .usp
bind uparrow say .deagle
bind downarrow say .ak
bind rightarrow	say .m4

bind INS say .p2000
bind HOME say .glock
bind PGUP say .galil
bind DEL say .scout
bind END say .m4a4
bind PGDN say .famas

alias refraghm "exec see_refrag.cfg"
exec see_refrag.cfg