 alias thiscfg "snd_voipvolume;cl_radar_scale; echo "[I] =========  REFRAG  == cfg ==== ""

bind mouse5 noclip
bind t say .clear
bind alt noclip

BIND F1 say .rethrow 0
bind F2 say .rethrow 1
bind F3 say .rethrow 2
bind F4 say .rethrow 3
bind F5 say .rethrow 4



alias pv say .preview
alias xr say .xray
alias nf say .noflash
alias imp say .impacts

echo "[I] //=============================//"
echo "[I] // .noflash NF | .impacts IMP |//"
echo "[I] // .preview PV | .xray  XR    |//"
echo "[I] //=============================//"

say aliases: pv - preview || xr - xray || nf - noflash || imp - impacts

bind y say .savepos //Z
bind u say .autoskip
bind i say .impact;
bind o say .noflash;

bind h say .goto	
bind j say .p; 
bind k say .replay; 
bind l say .n;

bind n say .bot
bind m say .dismiss
bind , say .preview;
bind . say .xray;

bind scancode100 say .stats;

echo "[I] //=====================================================//"
echo "[I] // Z savepos | U           | I impact   | O noflash   | //"
echo "[I] // H goto    | J .p (prev) | K .replay  | L .n (next) | //"
echo "[I] // N botadd  | M botrem    | , noflash  | . xray      | //"
echo "[I] //=====================================================//"
echo "[I] == scancode100 stats"
echo "[I] == F8 comp = F9 hard = F10 normal"

bind tab "+showscores;.session"

bind leftarrow say .usp
bind uparrow say .deagle
bind downarrow say .ak
bind rightarrow	say .m4

bind INS say .mp9
bind HOME say .mac
bind PGUP say .ump
bind DEL say .galil
bind END say .famas
bind PGDN say .m4a4 

// Stats
alias .stats say .stats
alias .session say .session
alias .xfire say .xfire

// Schwierigkeit
alias .comp say .comp
alias .hard say .hard
alias .normal say .normal
alias .cold say .cold
alias .smart say .smart
alias .elo say .elo 18000

// Type 
alias .default say .preset default;
alias .pistol say .preset pistol;
alias .save say .preset save;
alias .eco say .preset eco;
alias .force say .preset force;

// Mode
alias .xfire say .xfire;
alias .prefire say .prefire;
alias .spray say .spray;
alias .defender say .defender;
alias .aimbotz say .aimbotz;
alias .NADR  say .NADR;
alias .session say .session;
alias .stats say .stats;
alias .previousstats say .previousstats;

// Useful Commands
alias .autoreset say .autoreset;
alias .autoskip say .autoskip;
alias .countdown  say .countdown;

alias .noflash say .noflash; 
alias .preview say .preview; 
alias .xray say .xray; 
alias .impacts say .impacts; 

alias crefrag "exec see_refrag.cfg"
host_writeconfig
exec see_refrag.cfg

echo "Warmup inbetween: MAP .MAPNAME , "
alias was ".mm;.vc;exec see_refrag"
echo "[CFG] Refrag Configuration"